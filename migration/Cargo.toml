[package]
name = "migration"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "migration"
path = "src/lib.rs"

[dependencies]
sea-orm-migration = { version = "0.8.0" }
tokio = { version = "1.19.2", features = ["rt", "macros"] }
entity = { path = "../entity/" }
iam = { path = "../" }
chrono = "0.4.19"
